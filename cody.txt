From the project root, the main command is:

./start.sh
That’s the documented startup flow in README.md:


README.md
Lines 91-99
Simply boot the orchestrator script from the root workspace:
```bash
./start.sh
What happens next?

FastAPI boots up in the background on http://127.0.0.1:8000.
Vite dev (Frontend) server boots concurrently on http://127.0.0.1:5173.
The script automatically opens your default web browser to the dashboard.
If this is your first run, the setup commands in the repo are:
```bash
pip install -r backend/requirements.txt
cd frontend && npm install && cd ..
cp .env.example .env
./start.sh
Those come from README.md here:


README.md
Lines 66-82
### 2. Install Backend Dependencies
Run pip installer from the backend folder:
```bash
pip install -r backend/requirements.txt
3. Install Frontend Dependencies
Run npm installer from the frontend folder:

cd frontend && npm install && cd ..
4. Setup Environment Variables
Copy .env.example to .env in the project root:

cp .env.example .env
If `./stt.sh` gives a permissions error on your machine, use:
```bash
bash ./start.sh
The app should then be at:

Frontend: http://127.0.0.1:5173/
Backend: http://127.0.0.1:8000/
If you want, I can also point out the separate commands to run the backend and frontend individually instead of using start.sh.
